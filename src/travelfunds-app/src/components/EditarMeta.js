import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

const EditarMeta = () => {
  return <Text style={styles.meta}>Editar Meta em construção</Text>;
};

const styles = StyleSheet.create({
  meta: {
    color: 'white',
    padding: 20,
  },
});

export default EditarMeta;
