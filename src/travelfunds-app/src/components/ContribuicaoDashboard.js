import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

const ContribuicaoDashboard = () => {
  return <Text style={styles.meta}>Contribuicao dashboard em construção</Text>;
};

const styles = StyleSheet.create({
  meta: {
    color: 'white',
    padding: 20,
  },
});

export default ContribuicaoDashboard;
