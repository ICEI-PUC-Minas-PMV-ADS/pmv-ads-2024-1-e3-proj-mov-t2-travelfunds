import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

const MetaDashboard = () => {
  return <Text style={styles.meta}>Meta dashboard em construção</Text>;
};

const styles = StyleSheet.create({
  meta: {
    color: 'white',
    padding: 20,
  },
});

export default MetaDashboard;
